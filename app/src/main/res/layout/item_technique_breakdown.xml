<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp">

    <TextView
        android:id="@+id/techniqueName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:ellipsize="end"
        android:maxLines="1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/techniqueCount"
        app:layout_constraintWidth_percent="0.6"
        tools:text="Box Breathing" />

    <TextView
        android:id="@+id/techniqueCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/gray_500"
        android:textSize="14sp"
        android:textAlignment="textEnd"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/techniqueName"
        app:layout_constraintBottom_toBottomOf="@id/techniqueName"
        tools:text="5 (41%)" />

    <!-- Progress background -->
    <View
        android:id="@+id/techniqueProgressBackground"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginTop="8dp"
        android:background="#1AFFFFFF"
        app:layout_constraintTop_toBottomOf="@id/techniqueName" />

    <!-- Progress bar -->
    <View
        android:id="@+id/techniqueProgress"
        android:layout_width="100dp"
        android:layout_height="4dp"
        android:background="@color/cyan_400"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/techniqueProgressBackground"
        tools:layout_width="120dp" />

</androidx.constraintlayout.widget.ConstraintLayout>